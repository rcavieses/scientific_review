# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/


# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py



# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ── Credenciales y API keys ────────────────────────────────────────────────
# Directorio de secretos (contiene apikeys de Scopus, ScienceDirect, Anthropic)
secrets/

# Archivos de API keys sueltos (por si se crean fuera de secrets/)
*apikey*
*api_key*
*api-key*
anthropic-apikey
springer_key
openai_key*
*_token.txt
*_secret.txt

# Variables de entorno con credenciales
.env
.env.*
*.env

# ── Outputs del pipeline (datos generados, no código) ──────────────────────
/outputs/

# ── Archivos de datos y logs ───────────────────────────────────────────────
*.pdf
*.png
*.jpg
*.jpeg
*.doc
*.docx
*.docm
*.dot
*.dotx
*.ppt
*.pptx
*.pptm
*.pot
*.potx
*.csv
*.log

# ── Miscelánea del proyecto ────────────────────────────────────────────────
__pycache__
CLAUDE.md
search_logs/

# ── Colecciones de PDFs (locales, iteraciones previas) ────────────────────
/PDF/
/PDF_GOC/

# ── Directorios grandes de resultados de búsqueda (ahora en outputs/, ya gitignored) ─
# outputs/search_results/          ← cubierto por /outputs/
# outputs/search_results_canonical/ ← cubierto por /outputs/
# outputs/scopus_results/           ← cubierto por /outputs/
# outputs/sciencedirect_results/    ← cubierto por /outputs/

# ── Estado y progreso del pipeline (en outputs/state/) ───────────────────
outputs/state/

# ── Archivos de estado sueltos que puedan quedar en raíz ──────────────────
*.pid
paywall_articles.json
species_with_pdfs.json
species_with_pdfs.txt
species_in_index.txt
pdfs_indexed.txt

# ── Modelos de embeddings (se descargan localmente) ────────────────────────
models/
!models/.gitkeep